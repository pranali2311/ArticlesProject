extends layout

block content
        br
        br
        h1 #{title}
        ul.list-group
            each article,i in articles
                li.list-group-item
                    a(href="/articles/"+article._id)= article.title
