extends layout

block content
        br
        br
        br
        h1 #{title}
        ul.list-group
            each article,i in articles
                li.list-group-item
                    a(href="/article/"+article._id)= article.title
